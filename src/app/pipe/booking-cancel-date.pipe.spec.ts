import { BookingCancelDatePipe } from './booking-cancel-date.pipe';

describe('BookingCancelDatePipe', () => {
  it('create an instance', () => {
    const pipe = new BookingCancelDatePipe();
    expect(pipe).toBeTruthy();
  });
});

import { OrderBookDetailPipe } from './order-book-detail.pipe';

describe('OrderBookDetailPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderBookDetailPipe();
    expect(pipe).toBeTruthy();
  });
});

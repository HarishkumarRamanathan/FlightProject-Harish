import { BookingHistoryDatePipe } from './booking-history-date.pipe';

describe('BookingHistoryDatePipe', () => {
  it('create an instance', () => {
    const pipe = new BookingHistoryDatePipe();
    expect(pipe).toBeTruthy();
  });
});
